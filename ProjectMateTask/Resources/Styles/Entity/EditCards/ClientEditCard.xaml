<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:actors="clr-namespace:ProjectMateTask.DAL.Entities.Actors;assembly=ProjectMateTask.DAL"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">



    <!--  Карточка редактирования клиентов  -->
    <DataTemplate DataType="{x:Type actors:Client}" x:Key="EditClientCard">

        <StackPanel>

            <StackPanel.Resources>
                <Style BasedOn="{StaticResource MaterialDesignFloatingHintTextBox}" TargetType="TextBox">

                    <Setter Property="FontSize" Value="16" />

                    <Setter Property="IsEnabled" Value="False" />

                </Style>

            </StackPanel.Resources>

            <!--  Имя  -->
            <TextBox
                IsEnabled="{Binding Path=DataContext.IsEditMode, RelativeSource={RelativeSource AncestorType={x:Type ContentControl}}}"
                MaxLength="50"
                Text="{Binding Name}"
                materialDesign:TextFieldAssist.HasClearButton="True">
                <materialDesign:HintAssist.Hint>
                    <StackPanel Margin="-2,0,0,0" Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="Person" />
                        <TextBlock Text="Имя" />
                    </StackPanel>
                </materialDesign:HintAssist.Hint>
            </TextBox>

            <!--  Статус  -->
            <TextBox
                IsEnabled="{Binding Path=DataContext.IsEditMode, RelativeSource={RelativeSource AncestorType={x:Type ContentControl}}}"
                MaxLength="50"
                Text="{Binding Status.Name}"
                VerticalAlignment="Center"
                materialDesign:TextFieldAssist.HasClearButton="True">
                <materialDesign:HintAssist.Hint>
                    <StackPanel Margin="-2,0,0,0" Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="AccountStar" />
                        <TextBlock Text="Статус" />
                    </StackPanel>
                </materialDesign:HintAssist.Hint>
            </TextBox>

            <!--  Менеджер  -->
            <TextBox
                IsEnabled="{Binding Path=DataContext.IsEditMode, RelativeSource={RelativeSource AncestorType={x:Type ContentControl}}}"
                MaxLength="50"
                Text="{Binding Manager.Name}"
                VerticalAlignment="Center"
                materialDesign:TextFieldAssist.HasClearButton="True">
                <materialDesign:HintAssist.Hint>
                    <StackPanel Margin="-2,0,0,0" Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="AccountTie" />
                        <TextBlock Text="Менеджер" />
                    </StackPanel>
                </materialDesign:HintAssist.Hint>
            </TextBox>


            <!--  Купленные продукты  -->
            <Expander Header="Приобретенные товары">
                <ListView ItemTemplate="{DynamicResource SimpleProductCard}" ItemsSource="{Binding Products}" />
            </Expander>

        </StackPanel>








    </DataTemplate>



</ResourceDictionary>